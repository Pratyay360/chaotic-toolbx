- When debugging build failures, inspect the actual repo files (Containerfile, package lists) to ground the fix in real config rather than guessing from error output alone. Confidence: 0.8
- Prefers minimal, surgical edits to package lists/configs over adding complexity (priority flags, overrides) to preserve both conflicting packages. Confidence: 0.4
- Before pruning package lists against a base image, verifies what's actually in the base by delegating to a sub-agent that fetches the authoritative source (Dockerfile, nixpkgs source) rather than relying on memory or assumptions. Confidence: 0.8
- When the user gives a vague bug report (e.g. "make this working"), proactively runs the build/setup to determine whether things actually work before asking a clarifying question, then reports concrete observations (e.g. "build succeeded end-to-end, what's the symptom?"). Confidence: 0.85
- After building a container image, inspects the actual runtime state by running it (e.g. `buildah run`) and probing files like `/etc/passwd`, `/etc/sudoers`, group membership, package binaries — rather than trusting that a successful build means correct runtime behavior. Confidence: 0.85
- For non-trivial multi-step container/system fixes, reverts in-progress partial edits and presents a complete plan with diagnosis table + proposed changes + verification steps + risk note, awaiting approval before applying. Confidence: 0.8
- After getting plan approval (e.g. "continue"), proceeds straight to implementation without re-asking for confirmation, applying the previously-presented fix, rebuilding, and verifying end-to-end. Confidence: 0.85